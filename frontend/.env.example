# Cotação de referência para exibir R$ nos gráficos (1 US$ = X R$). Ajuste ao dia.
VITE_USD_BRL_RATE=5.5

# Produção / preview: URL pública da API (sem barra no fim).
# Dev local: deixe vazio — o Vite (vite.config) faz proxy /api → http://localhost:8001
# VITE_API_BASE_URL=https://mdi-project-production.up.railway.app
